# MongoDB Configuration (Required)
MONGODB_URI=mongodb+srv://jubayerat05_db_user:Jubayer12345@cluster0.aazzlym.mongodb.net/cheatlock?appName=Cluster0

# JWT Secret (Required)
JWT_SECRET=cheatlock-local-dev-jwt-secret-change-before-production

# Server Configuration (Optional)
PORT=3000
ALLOWED_ORIGINS=*

# MongoDB Database Name (Optional)
MONGODB_DB_NAME=cheatlock

# MongoDB DNS Servers (Optional)
# MONGODB_DNS_SERVERS=8.8.8.8,8.8.4.4
